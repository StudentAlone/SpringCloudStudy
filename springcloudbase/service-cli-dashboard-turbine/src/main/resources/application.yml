server:
  port: 9097

spring:
  application:
    name: service-cli-dashboard-turbine2

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9090/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
      cors:
        allowed-origins: "*"
        allowed-methods: "*"